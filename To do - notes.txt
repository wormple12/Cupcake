Færdiggør extreme overhaul: Alt html skal være i jsp-filer. Al logik skal være i java-klasser. Alle requests skal gå igennem front controlleren ved hjælp af en konvertering af vores servlets til commands.

redirectJSP.java: Skal den bruges til alt, eller skal den slettes?

Shopping Cart button should be in menu or in pageheader

/showuser info box is not like the other info boxes

image cleanup

all commands and jsp-pages: rename and put into folders for better overview

registration og login - javascript input validering

DAO.addOrder(): lineitemID og orderID skal være auto-incrementing i databasen. Random.nextInt kan i princippet ramme de samme tal igen, og id'erne er dermed ikke garanteret unikke.

Bedre exception handling (lige nu bruger vi bare e.printStackTrace())

JUnit Tests af klasserne

Command-class should use HashMap instead of switch